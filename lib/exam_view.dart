import 'package:flutter/material.dart';

class ExamView extends StatelessWidget {
  const ExamView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Soru Çöz'),
      ),
      body: Container(),
    );
  }
}
